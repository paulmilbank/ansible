[defaults]
# Increase timeout for password prompts
timeout = 30
command_timeout = 30

# Configure become (sudo) settings
[privilege_escalation]
become_method = sudo
become_user = root
become_ask_pass = True
become_exe = sudo.ws
